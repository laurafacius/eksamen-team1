---
const header = Astro.props.header
const text = Astro.props.text
import Button from "../components/Button.astro"
---
<article class="bg-lysegrøn300 grid grid-cols-2 gap-5 rounded-xl">

<div>
    <!-- img -->
    <slot/>
</div>

<div class=" pt-10 pr-5 md:pt-10 font-lato">
    <h4 class="text-base font-bold pb-3 md:text-2xl">{header}</h4>
    <p class="text-xs md:text-base md:pb-8">{text}</p>
            <div class="flex justify-start">
              <a href="/singleview"><Button variant="primary" size="medium"></Button></a>
        </div>
</div>

</article>