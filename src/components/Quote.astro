---

---
    <section class="-z-20">
        <div class=" bg-lysegrøn700 pt-20 pb-20 mx-auto px-5 md:px-0 -z-20">
        <p class="quote px-16 text-white font-Lato font-light text-center text-2xl leading-relaxed sm:text-2xl md:text-4xl lg:text-5xl sm:leading-loose md:leading-loose lg:leading-loose">
        <span class="font-bold font-Lato text-9xl text-center">"</span>Madpapir og æggeskaller pynter ej i skovens haller”</p>
        </div>
    </section>

<script>
// Importér funktionerne 'animate' og 'inView' fra "motion"-biblioteket
import { animate, inView } from "motion";

// Finder det første HTML-element med klassenavn '.quote' og gemmer det i variablen 'elementmedanimation1'
const elementmedanimation1 = document.querySelector(".quote");

// Bruger 'inView'-funktionen til at registrere, når 'elementmedanimation1' bliver synligt i viewport
inView(elementmedanimation1, () => {

// Når elementet bliver synligt, tilføjer CSS-klassen 'scale-up-center' til elementet
elementmedanimation1.classList.add("scale-up-center");

});  
</script>

<style>
.scale-up-center {
	-webkit-animation: scale-up-center 0.4s cubic-bezier(0.390, 0.575, 0.565, 1.000) both;
	        animation: scale-up-center 0.4s cubic-bezier(0.390, 0.575, 0.565, 1.000) both;
} 

/* ----------------------------------------------
 * Generated by Animista on 2024-5-29 17:58:6
 * Licensed under FreeBSD License.
 * See http://animista.net/license for more info. 
 * w: http://animista.net, t: @cssanimista
 * ---------------------------------------------- */

/**
 * ----------------------------------------
 * animation scale-up-center
 * ----------------------------------------
 */
@-webkit-keyframes scale-up-center {
  0% {
    -webkit-transform: scale(0.5);
            transform: scale(0.5);
  }
  100% {
    -webkit-transform: scale(1);
            transform: scale(1);
  }
}
@keyframes scale-up-center {
  0% {
    -webkit-transform: scale(0.5);
            transform: scale(0.5);
  }
  100% {
    -webkit-transform: scale(1);
            transform: scale(1);
  }
}

</style>